services:
  - type: web
    name: precios-ar
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: precios-ar-db
          property: connectionString
      - key: SECRET_KEY
        sync: false

databases:
  - name: precios-ar-db
    plan: free
